"""
Core modules for the evacuation planner application.

Used so that src is marked as a package, so imports like from src.planner import choose_best_plan
stay explicit and reliable across different environments.
"""
